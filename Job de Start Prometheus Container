pipeline {
    agent any

    stages {
        stage('Start Prometheus Container') {
            steps {
                script {
                    docker.container('prometheus')
                        .withRun('--name prometheus -p 9090:9090')
                        .run('prom/prometheus')
                }
            }
        }
    }
}
