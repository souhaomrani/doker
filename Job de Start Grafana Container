pipeline {
    agent any

    stages {
        stage('Start Grafana Container') {
            steps {
                script {
                    docker.container('grafana')
                        .withRun('--name grafana -p 3000:3000')
                        .run('grafana/grafana')
                }
            }
        }
    }
}
