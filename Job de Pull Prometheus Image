pipeline {
    agent any

    stages {
        stage('Pull Prometheus Image') {
            steps {
                script {
                    docker.image('prom/prometheus').pull()
                }
            }
        }
    }
}
